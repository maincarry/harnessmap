node_modules/
dist/
.env
*.log
harnessmap.sqlite*
.browserlibs/
# generated artifacts (regenerable):
docs/ontology/ontology-combined.md
docs/ui-screens.html
backups/

# M183 (Mark): design rationale stays local — the judgment record is not for the public repo
/DESIGN.md
/docs/

# raw conversation transcripts archived for runs — local only (M220: a key rode in one)
docs/archive/**/raw-source-*

# M222 (Mark): conversation history, run artifacts, logs and databases never go to GitHub
docs/archive/
src/eval/*segment*.json
*.jsonl
*ckpt*.json
*checkpoint*.json
*.sqlite
*.sqlite-wal
*.sqlite-shm
